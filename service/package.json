{
  "name": "service",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "bun run --cwd .. build:domain",
    "build": "bun run prebuild && tsc --noEmit",
    "pretest": "bun run prebuild",
    "test": "bun test",
    "dev": "bun run prebuild && bun run src/main.ts"
  }
}
