{
  "name": "event-store",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "aggregator-registration": "workspace:*",
    "database-repo": "workspace:*",
    "event-creation": "workspace:*",
    "event-delivery": "workspace:*",
    "event-obsolescence": "workspace:*",
    "hlc-clock": "workspace:*",
    "schema-migration": "workspace:*"
  },
  "scripts": {
    "prebuild": "bun run --cwd ../.. build:domain",
    "build": "bun run prebuild && tsc --noEmit",
    "pretest": "bun run prebuild",
    "test": "bun test"
  }
}
